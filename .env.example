# Optional — enables the (not-yet-live) AI interview scoring feature.
# See server/anthropic.js. Without this set, scoring is disabled and throws
# a clear error rather than silently doing nothing.
ANTHROPIC_API_KEY=
